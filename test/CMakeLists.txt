function(enable_geometry_test_static_analysis targetName)
    if (DEFINED ${PROJECT_NAME}_STATIC_ANALYSIS AND ${PROJECT_NAME}_STATIC_ANALYSIS)
        enable_static_analysis(${targetName} "${${PROJECT_NAME}_WARNINGS_AS_ERRORS}")
    endif ()
endfunction()

function(create_geometry_test testName)
    add_executable(${testName} ${testName}.cpp)
    target_link_libraries(${testName} PRIVATE gtest_main Geometry::Geometry)
    enable_geometry_test_static_analysis(${testName})
    add_test(NAME Geometry.${testName} COMMAND ${testName})
endfunction()

# Skip install test when coverage is enabled (coverage-instrumented libraries
# can't be linked by non-coverage builds, causing undefined gcov symbols)
if(NOT CMAKE_CXX_FLAGS MATCHES "--coverage")
    add_subdirectory(test_install)
endif()

add_subdirectory(Distance)
add_subdirectory(Intersection)
add_subdirectory(Transformation)

create_geometry_test(AABBTest)
create_geometry_test(CircleTest)
create_geometry_test(SphereTest)
create_geometry_test(GeomVecOperationsTest)
create_geometry_test(KdTreeTest)
create_geometry_test(LineTest)
create_geometry_test(PlaneTest)
create_geometry_test(RayTest)
create_geometry_test(SegmentTest)
create_geometry_test(KdTreeMapTest)
create_geometry_test(ConeTest)
create_geometry_test(CuboidTest)
create_geometry_test(TriangleTest)
create_geometry_test(HandleTest)
